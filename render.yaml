services:
  - type: web
    name: silent-class-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app -k eventlet -w 1"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
